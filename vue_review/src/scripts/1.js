/**
 *
 *   第一种
 * 将光标放置于function上面，快捷键是 Ctrl+Alt+D 加 Ctrl+Alt+D。
 *
 * @param {*} a
 * @param {*} b
 */
function test(a, b) {}

/**
 * 在函数名后添加/**后,回车
 *
 *
 * @param {*} a
 * @param {*} b
 */
function ba(a, b) {}